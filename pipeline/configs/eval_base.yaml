model:
  path: /scratch-shared/sperdijk/storylm_p1_training/models/base_gpt2_60m_16k_epoch1/final
  tokenizer_path: /scratch-shared/sperdijk/storylm_p1_training/models/base_gpt2_60m_16k_epoch1/final

data:
  config: data_base.yaml   # resolved relative to this file's directory
  split: test               # dev | test

eval_batch_size: 16

paths:
  output_dir: /scratch-shared/sperdijk/storylm_p1_training/results/base_gpt2_60m_16k_epoch1

wandb:
  entity: sabijn-leiden-university
  project: storytimelm-pretraining
  run_name: eval_base_gpt2_60m_16k_epoch1
  tags: [eval, base, gpt2, 60m, tok16k, epoch1]

blimp:
  normalize_by_length: true
