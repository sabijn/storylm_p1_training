model:
  path: /scratch-shared/sperdijk/storylm_p1_training/models/base_gpt2_60m_32k_run3/final
  tokenizer_path: /scratch-shared/sperdijk/storylm_p1_training/models/base_gpt2_60m_32k_run3/final

data:
  config: data_base.yaml   # resolved relative to this file's directory
  split: test               # dev | test

eval_batch_size: 16

paths:
  output_dir: /scratch-shared/sperdijk/storylm_p1_training/results/base_gpt2_60m_32k_run3

wandb:
  entity: sabijn-leiden-university
  project: storytimelm-pretraining
  run_name: eval_base_gpt2_60m_32k_run3
  tags: [eval, base, gpt2, 60m, tok_32k, run3]

blimp:
  normalize_by_length: true
