# Data used to pretrain the base model from scratch, and (by default) to train the tokenizer.
sources:
  - name: babylm_nl
    type: hf
    hf_name: "BabyLM-community/babylm-nld"
    split: train
    text_column: text

split:
  dev_fraction: 0.05
  test_fraction: 0.05
  seed: 42

paths:
  output_dir: scratch-shared/sperdijk/storylm_p1_training/data/base_pretrain
